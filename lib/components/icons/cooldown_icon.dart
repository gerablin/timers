import 'package:flutter/material.dart';

class CooldownIcon extends StatelessWidget {
  const CooldownIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.ac_unit_outlined,
      color: Colors.lightBlueAccent,
    );
  }
}
